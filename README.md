# monte-carlo-spice

## 1. Project Setup (File Structure)
- Created the complete project folder structure

## 2. Installed Dependencies
### ✅ vcpkg (C++ Package Manager)

### Cloned to C:\\vcpkg
- Bootstrapped and ready to use `cd C:\\vcpkg`

### ✅ Eigen3 Library

### Installed via vcpkg
This is the math library for matrix operations (Newton-Raphson solver needs this)

✅ CMake (Build System)

## Installed version 4.3.0
- Added to PATH so you can run cmake commands
- Working in both Windows PowerShell and VS Code

## 3. Current Status
✅ Empty project structure
✅ All dependencies installed
✅ Git initialized in project folder
